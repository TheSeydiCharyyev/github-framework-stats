<svg xmlns="http://www.w3.org/2000/svg" width="{{ width }}" height="{{ height }}" viewBox="0 0 {{ width }} {{ height }}">
  <defs>
    <style>
      .title { font: 600 14px system-ui, -apple-system, sans-serif; fill: {{ theme.text_primary }}; }
      .lang-name { font: 400 11px system-ui, -apple-system, sans-serif; fill: {{ theme.text_primary }}; }
      .lang-pct { font: 400 11px system-ui, -apple-system, sans-serif; fill: {{ theme.text_primary }}; }
    </style>
  </defs>

  <rect width="100%" height="100%" fill="{{ theme.background }}" rx="6"{% if not hide_border %} stroke="{{ theme.border }}" stroke-width="1"{% endif %}/>

  <!-- Title -->
  <text x="25" y="35" class="title">Most Used Technologies</text>

  {% set total = technologies|sum(attribute='count') %}
  {% set bar_width = width - 50 %}
  {% set max_pct = (technologies[0].count / total * 100) if total > 0 and technologies else 0 %}

  {% for tech in technologies %}
  {% set pct = (tech.count / total * 100) if total > 0 else 0 %}
  {% set item_y = 55 + loop.index0 * 40 %}

  <!-- {{ tech.name }} -->
  <g transform="translate(25, {{ item_y }})">
    {% set icon_data = get_icon_data_uri(tech.icon) %}
    {% if icon_data %}
    <image x="0" y="-11" width="14" height="14" href="{{ icon_data }}"/>
    {% else %}
    <circle cx="7" cy="-4" r="6" fill="{{ tech.color }}"/>
    {% endif %}
    <text x="20" y="0" class="lang-name">{{ tech.name }}</text>
    <text x="{{ bar_width }}" y="0" text-anchor="end" class="lang-pct">{{ "%.2f"|format(pct) }}%</text>
    <rect x="0" y="8" width="{{ bar_width }}" height="8" rx="4" fill="{{ theme.border }}" opacity="0.4"/>
    <rect x="0" y="8" width="{{ (pct / max_pct * bar_width) if max_pct > 0 else 0 }}" height="8" rx="4" fill="{{ tech.color }}"/>
  </g>
  {% endfor %}
</svg>
